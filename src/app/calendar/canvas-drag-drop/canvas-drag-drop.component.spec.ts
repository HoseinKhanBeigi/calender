import { ComponentFixture, TestBed } from '@angular/core/testing';

import { CanvasDragDropComponent } from './canvas-drag-drop.component';

describe('CanvasDragDropComponent', () => {
  // let component: CanvasDragDropComponent;
  // let fixture: ComponentFixture<CanvasDragDropComponent>;
  // beforeEach(async () => {
  // await TestBed.configureTestingModule({
  //   imports: [CanvasDragDropComponent]
  // })
  // .compileComponents();
  // fixture = TestBed.createComponent(CanvasDragDropComponent);
  // component = fixture.componentInstance;
  // fixture.detectChanges();
  // });
  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });
});
